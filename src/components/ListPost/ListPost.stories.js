import React from 'react';

import ListPost from './ListPost'

export default {
  title: 'Components/ListPost',
  component: ListPost,
};

const Template = (args) => <ListPost {...args} />;

export const Default = Template.bind({});


